% Erick Blankenberg
% DeLaZerda Research
% Fluoroscope
% Panorama_Manual

% This program allows the user to create a large tiled image by manually
% adjusting the X and Y direction
% 
% Uses image stitching described by: https://www.mathworks.com/help/vision/examples/feature-based-panoramic-image-stitching.html
%

% ---------------------------------- Settings -----------------------------



% -------------------------------- The Program ----------------------------

% -> Sets up GUI and Camera
figure(1)
hold on;
hold off;

% -> Streams the latest images from the camera, allows the user to take an
%    image to save, append to the current panorama, etc.
exitProgram = false;
while(~exitProgram) % Main loop just shows camera footage
    
end

% --------------------------------- Functions -----------------------------

% Retrieves a high quality image
function newImage = main_getNextImage(numAverages)
    newImage = [];
    for averagingIndex = 1:numAverages
        newImage = newImage + acquireFrame();
    end
    newImage = newImage./numAverages;
end
